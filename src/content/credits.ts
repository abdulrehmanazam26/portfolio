export type Credit = {
  label: string;
  url: string;
};

/**
 * TODO(abdulrehman): populate with real attribution once stock imagery is
 * chosen (Unsplash / Pexels only — check each image's licence). Rendered in
 * the footer.
 */
export const credits: Credit[] = [];
